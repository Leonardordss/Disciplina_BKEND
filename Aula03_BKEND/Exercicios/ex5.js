const frase = "Olá, Mundo!";

const upperCaseFrase = frase.toUpperCase();
const lowerCaseFrase = frase.toLowerCase();
const slicedFrase = frase.slice(4, 9);
const replacedFrase = frase.replace("Mundo", "Universo");

console.log(upperCaseFrase);
console.log(lowerCaseFrase);
console.log(slicedFrase);
console.log(replacedFrase);
